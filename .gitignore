# Secrets — NEVER commit
.env
.env.*
!.env.example
# RESUME.md contains real keys — keep local only
RESUME.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.python-version

# Testing / cache
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store

# Fly.io — fly.toml is non-secret config and SHOULD be committed.
# Secrets are set via `fly secrets set`, never stored in fly.toml.
